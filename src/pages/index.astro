<button onclick="generate('lugo')">Ready for Lugo (10s)</button>
<button onclick="generate('pixverse')">Ready for Pixverse (8s)</button>

<script>
  function generate(service) {
    // Quando dici "ready for Lugo", l'animazione deve essere di 10 secondi.
    // Quando dici "ready for Pixverse", l'animazione deve essere di 8 secondi.
    const duration = (service === 'lugo') ? 10 : 8;
    console.log(`Generando video con ${service} per ${duration} secondi...`);
    // Qui andrà la tua chiamata al Worker
  }
</script>
